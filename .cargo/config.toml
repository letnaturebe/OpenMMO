[env]
# Public OAuth client id (also shipped to browsers via VITE_GOOGLE_CLIENT_ID).
# A var already exported in the shell takes precedence over this.
GOOGLE_CLIENT_ID = "1080165942907-fht8nlv17kpod7vbhoq4m9d9innpbboh.apps.googleusercontent.com"
# Private values (ADMIN_EMAILS, ...) go in ~/.cargo/config.toml, not here —
# this file is tracked in a public repo.
